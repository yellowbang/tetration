import {Icon} from 'cisco-ui-components';
import {PATHS} from './route-config';

const sidebarItems = [
    {
        id: 'tetration',
        icon: Icon.TYPE.REPORT,
        title: 'Tetration',
        path: PATHS.Tetration
    }
];

export default sidebarItems;
